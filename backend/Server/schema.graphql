type Query {
    samplecard: [Card]
    cardsInCountyState(countyInput: String!, stateInput: String): [Card]
}

type Card {
    name: String
    address: String
    phone: String
    description: String
    source: String
    longitude: Float
    latitude: Float
}
